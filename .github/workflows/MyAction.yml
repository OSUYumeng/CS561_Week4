# name: Test With My Action
# on:
#   push:
#     banches:
#       -Use_Action
# jobs: 
#   build_my_swift:
#     runs-on: ubuntu-latest
    
#     name: Build the Swift
#     run: swift build

#     name: Start the test
#     run: swift test

name: Test With My Action
on: 
  push:
    branches: ["Yumeng/use-actions"]

jobs:
  my-job:

    runs-on: ubuntu-latest

    steps:

      # - name: Do excutable everywhere
      #   run: swift package init --type executable
      # - name: check pwd
      #   run: pwd
      # - name: check ls
      #   run: ls -a
      - name: go previous
        run: cd ..; ls -a
      - name: Do Build
        working-directory: ./MyLibrary
        run: swift build -v
      - name: Do Test
        working-directory: ./MyLibrary
        run: swift test -v